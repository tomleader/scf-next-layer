{
  "name": "nextjs-scf-layer",
  "scripts": {
    "build": "",
    "build-zip": "mkdir ./dist && zip -rq layer.zip ./node_modules/ && mv layer.zip ./dist && ls -al ./ && ls -al ./dist"
  },
  "dependencies": {
    "next": "15.4.5",
    "react": "19.1.1",
    "react-dom": "19.1.1"
  }
}
